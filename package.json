{
  "scripts": {
    "build": "cd client && bun run build",
    "start": "cd server && bun run start",
    "buncompile": "bun build --compile server/src/index.ts --outfile=projectcompetentia"
  },
  "workspaces": [
    "client",
    "server"
  ]
}